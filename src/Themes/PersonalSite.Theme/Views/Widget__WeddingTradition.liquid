{% assign title = Model.ContentItem.Content.WeddingTradition.Title.Text %}
{% assign description = Model.ContentItem.Content.WeddingTradition.Description.Text %}

<article>
    <h3 class="font-display text-2xl uppercase tracking-widest text-gold-dark">{{ title }}</h3>
    <p class="mt-2 text-lg">{{ description }}</p>
</article>
