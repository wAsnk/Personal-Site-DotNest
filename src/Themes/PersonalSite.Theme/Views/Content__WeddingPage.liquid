{% assign bag_items = Model.ContentItem.Content.BagPart.ContentItems %}

{% if bag_items %}
    {% for item in bag_items %}
        {% assign theme_value = item.BlockCommon.Theme.Text %}
        {% assign theme_key = "light-gray" %}

        {% case theme_value %}
            {% when "DarkBlue" %}{% assign theme_key = "dark-blue" %}
            {% when "DarkPanel" %}{% assign theme_key = "dark-panel" %}
            {% when "White" %}{% assign theme_key = "white" %}
        {% endcase %}

        <div data-theme="{{ theme_key }}">
            {{ item | shape_build_display: "Detail" | shape_render }}
        </div>
    {% endfor %}
{% endif %}
